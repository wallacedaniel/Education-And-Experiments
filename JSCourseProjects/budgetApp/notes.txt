



planning step 1
  TO DO list
    Add Event handler
    Get Input values
    Add the new item to data structure
    Add the new item to the UI
    Calculate the budget
    Update the UI

Modules
  UI Module
    Get Input values
    Add the new item to the UI
    Update the UI
  Data Module
    Add the new item to data structure
    Calculate the budget
  Controller Module
    Add Event handler

  Module Pattern
    via IIFE .. immediately invoked functions ... creates new scope
    functions defined within are private
    returns object with functions to be made public
    works because of closures ...after module function has executed .. inner function still has access to already returned outer function variables
    pass other modules into modules as parameters rather than accessing from the main scope

  Setup Event Listeners
  Read Input data
  Initialization Function
